# Environment variables — never commit these
.env

# Pre-built catalog snapshot — rebuilt by Railway on startup
catalog_cache.json

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
venv/
.venv/
env/
